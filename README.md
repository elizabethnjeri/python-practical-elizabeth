# Python Practical Assignment

Name: Elizabeth njeri
Admission Number:T006/304067/2024

## Description of Files

| File | Description |
|---|---|
| `integer.py` | Demonstrates integer variables and arithmetic operators (+, -, *, /, %, //). |
| `float.py` | Demonstrates float variables and calculations involving decimal numbers. |
| `boolean.py` | Demonstrates Boolean variables and comparison operators (==, !=, >, <, >=, <=). |
| `string.py` | Demonstrates string concatenation, indexing, slicing, and string methods. |
| `list.py` | Demonstrates creating, adding to, removing from, updating, and looping through a list. |
| `tuple.py` | Demonstrates tuple creation, accessing elements, and tuple immutability. |
| `set.py` | Demonstrates set creation, adding/removing elements, and automatic duplicate removal. |
| `dictionary.py` | Demonstrates creating a student dictionary and accessing/updating its values. |
| `selection.py` | Demonstrates if, if...else, and if...elif...else statements with pass/fail, even/odd, and largest-of-three examples. |
| `looping.py` | Demonstrates for and while loops, including a multiplication table and sum calculations. |
| `builtin_functions.py` | Demonstrates at least 10 Python built-in functions such as len(), max(), min(), sum(), and sorted(). |
| `user_defined_functions.py` | Demonstrates user-defined functions for addition, rectangle area, prime checking, factorial, and displaying student details. |
| `lambda_functions.py` | Demonstrates lambda functions for addition, multiplication, sorting, filter(), and map(). |
| `classes.py` | Demonstrates a basic `Student` class with attributes and multiple objects. |
| `class_variables.py` | Demonstrates a class variable (school name) shared across multiple objects. |
| `class_methods.py` | Demonstrates a constructor, instance/class variables, and a `@classmethod`. |

## How to Run

Each file can be run independently from the terminal:

```bash
python filename.py
```

Example:

```bash
python integer.py
```

